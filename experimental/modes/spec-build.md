# Preset: spec-build (reference)

Use when spec and plan/tasks exist and implementation is the main focus.

- Invoke **`40-executing-plan`** and keep **`state.json`** truthful.
- Escalate to **`30-writing-plan`** if scope or tasks must change materially.
