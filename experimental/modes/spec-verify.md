# Preset: spec-verify (reference)

Use when implementation is claimed done and evidence must be recorded.

- Invoke **`50-verification-before-completion`** then **`60-closing-feature`**.
- Treat **score ≥ 90** as the default completion bar.
