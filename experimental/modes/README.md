# Experimental: Custom Modes presets (not installed)

Cursor **Custom Modes** are **beta**. These files are **reference only** for maintainers and power users.

They are **not** copied into consumer projects by `spec-driven-kit install` (v1).

See the product README **compatibility matrix** for stable vs beta surfaces.
