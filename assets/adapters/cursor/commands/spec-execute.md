# Spec execute

Execute as tasks aprovadas da feature.

## Instruções

1. Confirme que `plan.md` e `tasks.md` existem e batem com `spec.md`.
2. Siga a regra `40-executing-plan`.
3. Atualize `state.json` e `decision-log.md` conforme o progresso real.
