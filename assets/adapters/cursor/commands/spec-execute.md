# Spec execute

Avance a fase real para execute e entregue a implementação.

## Instruções

1. Rode `elf phase execute <run-id>`.
2. Confirme que o próximo passo virou `verify`.
3. Execute as tasks em `.elf/phases/<phase-id>/tasks.md`.
4. Atualize `.elf/phases/<phase-id>/decision-log.md` conforme o progresso real.
