# Spec execute

Execute approved tasks for the feature.

## Instructions

1. Confirm `plan.md` and `tasks.md` exist and align with `spec.md`.
2. Apply Project Rule **`40-executing-plan`**.
3. Update `state.json` and `decision-log.md` as you progress.
