# Spec plan

Run the **plan and tasks** phase.

## Instructions

1. Confirm `spec.md` exists and is sufficient.
2. Apply Project Rule **`30-writing-plan`**.
3. Produce `plan.md` and `tasks.md` with explicit, ordered, actionable tasks.
4. Do not start implementation until both files satisfy the validity checks in that rule.
