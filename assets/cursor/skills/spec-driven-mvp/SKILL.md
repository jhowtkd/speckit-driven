---
name: spec-driven-mvp
description: >-
  This repo uses the spec-driven kit. Follow Project Rules in .cursor/rules and
  AGENTS.md at the repository root. No global framework path required.
---

# Spec-driven kit (Cursor)

Use **`.cursor/rules/*.mdc`** as the operational source of truth. Read **`AGENTS.md`** at the repo root for precedence and entry points.

Feature work lives under **`.cursor/features/<FEATURE_ID>/`** with templates from **`.cursor/templates/`**.
